\documentclass{article}
\usepackage{amsmath,amssymb,braket,bbm}
\usepackage[margin=2.5cm]{geometry}

\begin{document}

\section{FM Heisenberg Chain}

\subsection{Real Space}

Hamiltonian:
\begin{align}
	\hat H = J \sum_i \vec S_i \cdot \vec S_{i+1}
\end{align}
with $J < 0$. \\
The ground state is a fully polarized ferromagnet:
\begin{align}
	\vec S_i = S \begin{pmatrix}
		0 \\ 0 \\ 1
	\end{pmatrix}
\end{align}
HP mapping:
\begin{align}
	S_{i}^z &= S - a_{i}^\dagger a_{i} \\
	%
	S_{i}^+ &= \sqrt{2S - a_{i}^\dagger a_{i}} a_{i} = \sqrt{2} \left(\sqrt{S} a_{i} - \frac{1}{4\sqrt{S}} a_{i}^\dagger a_{i} a_{i} + \dots \right) \\
	%
	S_{i}^- &= a_{i}^\dagger \sqrt{2S - a_{i}^\dagger a_{i}} = \sqrt{2} \left(\sqrt{S} a^\dagger_{i} - \frac{1}{4\sqrt{S}} a_{i}^\dagger a^\dagger_{i} a_{i} + \dots \right) \\
\end{align}
The Heisenberg term becomes:
\begin{align}
	\dots &= S_i^z S_{i+1}^z + \frac{1}{2} \left(S_i^+ S_i^- + h.c.\right) \\
%
	&= (S - a_i^\dagger a_i) (S - a_{i+1}^\dagger a_{i+1}) + \left(\left(\sqrt S a_i - \frac{1}{4\sqrt S} a_i^\dagger a_i a_i + h.c.\right) \left(\sqrt S a_{i+1}^\dagger - \frac{1}{4\sqrt S} a_{i+1}^\dagger a_{i+1}^\dagger a_{i+1} + \dots\right) + h.c. \right) \\
%
	&= S^2 - S(a_i^\dagger a_i + a_{i+1}^\dagger a_{i+1}) + a_i^\dagger a_i a_{i+1}^\dagger a_{i+1} + \left( S a_i a_{i+1}^\dagger - \frac{1}{4} a_i^\dagger a_i a_i a_{i+1}^\dagger - \frac{1}{4} a_i a_{i+1}^\dagger a_{i+1}^\dagger a_{i+1} + h.c. + \dots \right)
\end{align}

\subsection{Momentum Space}
Fourier transform:
\begin{align}
	a_i = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_k \hspace{2cm} a_i^\dagger = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_{-k}^\dagger
\end{align}
Quadratic Hamiltonian:
\begin{align}
	\hat H_2 &= JS \sum_i \left( -a_i^\dagger a_i - a_{i+1}^\dagger a_{i+1} + a_i a_{i+1}^\dagger + a_{i+1} a_i^\dagger \right) \\
%
	&= JS \frac{1}{N} \sum_{kq} \sum_i e^{i (k+q) x_i} \left( -a_{-k}^\dagger a_q - e^{i(k + q) a} a_{-k}^\dagger a_q + e^{iq} a_{k} a_{-q}^\dagger + e^{ik} a_{k} a_{-q}^\dagger \right) \\
%
	&= JS \sum_{kq} \delta_{k+q,0} \left( -a_{-k}^\dagger a_q - e^{i(k + q)} a_{-k}^\dagger a_q + e^{iq} a_{k} a_{-q}^\dagger + e^{ik} a_{k} a_{-q}^\dagger \right) \\
%
	&= JS \sum_{kq} \delta_{k+q,0} \left( -(1 + e^{i(k + q)}) a_{-k}^\dagger a_q + (e^{iq} + e^{ik}) a_{k} a_{-q}^\dagger \right) \\
%
	&= JS \sum_{kq} \delta_{k+q,0} \begin{pmatrix} a_k & a_{-k}^\dagger \end{pmatrix} \begin{pmatrix}
		0 & e^{iq} + e^{ik} \\ -(1 + e^{i(k+q)}) & 0
	\end{pmatrix} \begin{pmatrix} a_q \\ a_{-q}^\dagger \end{pmatrix}
\end{align}
Quartic Hamiltonian:
\begin{align}
	\hat H_4 &= J \sum_i \left(a_i^\dagger a_i a_{i+1}^\dagger a_{i+1} - \frac{1}{4} \left( a_i^\dagger a_i a_i a_{i+1}^\dagger + a_i a_{i+1}^\dagger a_{i+1}^\dagger a_{i+1} + a_{i+1} a_i^\dagger a_i^\dagger a_i + a_{i+1}^\dagger a_{i+1} a_{i+1} a_i^\dagger \right) \right) \\
%
	&= J \frac{1}{N^2} \sum_{klpq} \sum_i e^{i(k+l+p+q)x_i} \bigg( e^{i(p+q)} a_{-k}^\dagger a_l a_{-p}^\dagger a_q - \frac{1}{4} \Big( e^{iq} a_{-k}^\dagger a_l a_p a_{-q}^\dagger + e^{i(l+p+q)} a_k a_{-l}^\dagger a_{-p}^\dagger a_q \\
	&\hspace{8cm} + e^{ik} a_k a_{-l}^\dagger a_{-p}^\dagger a_q + e^{i(k+l+p)} a_{-k}^\dagger a_l a_p a_{-q}^\dagger \Big) \bigg) \\
%
	&= J \frac{1}{N} \sum_{klpq} \delta_{k+l+p+q,0} \left( e^{i(p+q)} a_{-k}^\dagger a_l a_{-p}^\dagger a_q - \frac{1}{4} \left( \left( e^{iq} + e^{i(k+l+p)} \right) a_{-k}^\dagger a_l a_p a_{-q}^\dagger + \left(e^{i(l+p+q)} + e^{ik}\right) a_k a_{-l}^\dagger a_{-p}^\dagger a_q \right) \right)
\end{align}

\subsection{LSWT}
We transform $\hat H_2$ to BdG form by (1) using the $\delta_{k+q,0}$ to kill the sum over $k$; (2) switching the order of the creators/annihilators in the row Nambu spinor appropriately; and (3) symmetrizing $q \leftrightarrow -q$. In general:
\begin{tiny}
\begin{align}
	\hat H_2 &= \sum_{kq} \delta_{k+q,0} \begin{pmatrix}
		a_{k1} & a_{-k1}^\dagger & 	a_{k2} & a_{-k2}^\dagger & \dots
	\end{pmatrix} \mathcal H_2(k, q) \begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} \\
%
	&\overset{(1)}{=} \sum_{q} \begin{pmatrix}
		a_{-q1} & a_{q1}^\dagger & 	a_{-q2} & a_{q2}^\dagger & \dots
	\end{pmatrix} \mathcal H_2(-q, q) \begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} \\
%
	&\overset{(2)}{=} \sum_{q} \begin{pmatrix} 
		a_{q1}^\dagger & a_{-q1} & a_{q2}^\dagger & a_{-q2} & \dots 
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \mathcal H_2(-q, q) \begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} \\
%
	&\overset{(3)}{=} \sum_{q>0} \left[ \begin{pmatrix} 
		a_{q1}^\dagger & a_{-q1} & a_{q2}^\dagger & a_{-q2} & \dots 
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \mathcal H_2(-q, q) \begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} + \begin{pmatrix} 
		a_{-q1}^\dagger & a_{q1} & a_{-q2}^\dagger & a_{q2} & \dots 
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \mathcal H_2(q, -q) \begin{pmatrix}
		a_{-q1} \\ a_{q1}^\dagger \\ a_{-q2} \\ a_{q2}^\dagger \\ \dots
	\end{pmatrix} \right] \nonumber \\
%
	&= \sum_{q>0} \begin{pmatrix} 
		a_{q1}^\dagger & a_{-q1} & a_{q2}^\dagger & a_{-q2} & \dots 
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \left( \mathcal H_2(-q, q) + \mathcal H_2^T(q, -q) \right) \begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} \\
\end{align}
\end{tiny}
where we have used that
\begin{align}
	&\begin{pmatrix} 
		a_{-q1}^\dagger & a_{q1} & a_{-q2}^\dagger & a_{q2} & \dots 
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \mathcal H_2(q, -q) \begin{pmatrix}
		a_{-q1} \\ a_{q1}^\dagger \\ a_{-q2} \\ a_{q2}^\dagger \\ \dots
	\end{pmatrix} \\
%
	=& \begin{pmatrix}
		a_{q1} & a_{-q1}^\dagger & a_{q2} & a_{-q2}^\dagger & \dots
	\end{pmatrix} \mathcal H_2^T(q, -q) (\mathbbm 1 \otimes \sigma_x) \begin{pmatrix} 
		a_{q1}^\dagger \\ a_{-q1} \\ a_{q2}^\dagger \\ a_{-q2} \\ \dots 
	\end{pmatrix} \nonumber \\
%
	=& \begin{pmatrix}
		a_{-q1}^\dagger & a_{q1} & a_{-q2}^\dagger & a_{q2} & \dots
	\end{pmatrix} (\mathbbm 1 \otimes \sigma_x) \mathcal H_2^T(q, -q) \begin{pmatrix} 
		a_{-q1} \\ a_{q1}^\dagger \\ a_{-q2} \\ a_{q2}^\dagger \\ \dots 
	\end{pmatrix} \nonumber 
\end{align}
For the actual model:
\begin{align}
	\hat H_2 &= JS \sum_q \begin{pmatrix}
		a_{-q} & a_{q}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & e^{iq} + e^{-iq} \\
		-2 & 0
	\end{pmatrix} \begin{pmatrix}
		a_q \\ a_{-q}^\dagger
	\end{pmatrix} \\
%
	&= 2JS \sum_q \begin{pmatrix}
		a_{q}^\dagger & a_{-q}
	\end{pmatrix} \begin{pmatrix}
		-1 + \cos(q) & 0 \\
		0 & -1 + \cos(q)
	\end{pmatrix} \begin{pmatrix}
		a_q \\ a_{-q}^\dagger
	\end{pmatrix}
\end{align}
The eigenenergies are $\epsilon_q = -2JS(1 - \cos(q))$. \\
Introduce the eigenmodes:
\begin{align}
	\begin{pmatrix}
		a_{q1} \\ a_{-q1}^\dagger \\ a_{q2} \\ a_{-q2}^\dagger \\ \dots
	\end{pmatrix} = \mathcal U(q) \begin{pmatrix}
		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger \\ \dots
	\end{pmatrix} \qquad \text{where} \quad \mathcal U(-q) = (\mathbbm 1 \otimes \sigma_x) \mathcal U(q)^* (\mathbbm 1 \otimes \sigma_x)
\end{align}
For the actual model: $\mathcal U(q) = \mathbbm 1$.
%so that
%\begin{align}
%	\hat H_2 &= \sum_q \begin{pmatrix}
%		\alpha_{-q1} & \alpha_{q1}^\dagger & \alpha_{-q2} & \alpha_{q2}^\dagger & \dots
%	\end{pmatrix} \underbrace{\mathcal U(-q)^T \mathcal H_2(-q, q) \mathcal U(q)}_{\substack{= (\mathbbm 1 \otimes \sigma_x) \mathcal U(q)^\dagger (\mathbbm 1 \otimes \sigma_x) \mathcal H_2(-q, q) \mathcal U(q) \\ \equiv \mathfrak h(q)}} \begin{pmatrix}
%		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger \\ \dots
%	\end{pmatrix} \\
%%
%	&= \sum_{q > 0} \begin{pmatrix}
%		\alpha_{-q1} & \alpha_{q1}^\dagger & \alpha_{-q2} & \alpha_{q2}^\dagger & \dots
%	\end{pmatrix} (\mathfrak h(q) + \mathfrak h^T(-q)) \begin{pmatrix}
%		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger \\ \dots
%	\end{pmatrix} \\
%%
%	 &= \sum_{q > 0} \begin{pmatrix}
%		\alpha_{-q1} & \alpha_{q1}^\dagger & \alpha_{-q2} & \alpha_{q2}^\dagger & \dots
%	\end{pmatrix} \begin{pmatrix}
%		0 & \epsilon_{-q1} & 0 & 0 & \dots \\
%		\epsilon_{q1} & 0 & 0 & 0 & \dots \\
%		0 & 0 & 0 & \epsilon_{-q2} & \dots \\
%		0 & 0 & \epsilon_{q2} & 0 & \dots \\
%		\vdots & \vdots & \vdots & \vdots & \ddots
% 	\end{pmatrix} \begin{pmatrix}
%		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger \\ \dots
%	\end{pmatrix}
%\end{align}

\subsection{Eigenspace}

Quadratic Hamiltonian:
\begin{align}
	\hat H_2 &= JS \sum_q \begin{pmatrix}
		\alpha_{-q} & \alpha_{q}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & e^{iq} + e^{-iq} \\
		-2 & 0
	\end{pmatrix} \begin{pmatrix}
		\alpha_q \\ \alpha_{-q}^\dagger
	\end{pmatrix}
\end{align}
Quartic Hamiltonian:
\begin{align}
	\hat H_4 &= J \frac{1}{N} \sum_{kpq} \left( e^{i(p+q)} a_{k+p+q}^\dagger a_{k} a_{-p}^\dagger a_q - \frac{1}{4} \left( \left( e^{iq} + e^{-iq} \right) a_{k+p+q}^\dagger a_{k} a_p a_{-q}^\dagger + \left(e^{i(k+p+q)} + e^{-i(k+p+q)}\right) a_{-k-p-q} a_{-k}^\dagger a_{-p}^\dagger a_q \right) \right) \nonumber \\
%
	&= J \frac{1}{N} \sum_{kpq} \left( e^{i(p+q)} \alpha_{k+p+q}^\dagger \alpha_{k} \alpha_{-p}^\dagger \alpha_q - \frac{1}{2} \left( \cos(q) \alpha_{k+p+q}^\dagger \alpha_{k} \alpha_p \alpha_{-q}^\dagger + \cos(k+p+q) \alpha_{-k-p-q} \alpha_{-k}^\dagger \alpha_{-p}^\dagger \alpha_q \right) \right) \nonumber
\end{align}

\subsection{Normal Order}
Constant commutator terms from the quadratic vertex:
\begin{align}
	\hat H_2 &= JS \sum_q \left( -2\alpha_{q}^\dagger \alpha_{q} + 2\cos(q) \alpha_{-q} \alpha_{-q}^\dagger \right) \\
%
	&= JS \sum_q \left( -2\alpha_{q}^\dagger \alpha_{q} + 2\cos(q) \alpha_{-q}^\dagger \alpha_{-q} + 2\cos(q) \right) \\
%
	&= \dots + 2JS \sum_q \cos(q)
\end{align}
Quadratic commutator terms from the quartic vertex:
\begin{tiny}
\begin{align}
	\hat H_4 &= \dots + \frac{J}{N} \sum_{kpq} \left( e^{i(p+q)} \alpha_{k+p+q}^\dagger \alpha_q \delta_{k,-p} - \frac{1}{2} \left( \cos(q) \alpha_{k+p+q}^\dagger \left( \alpha_k \delta_{p,-q} + \alpha_p \delta_{k,-q} \right) + \cos(k+p+q) \left(\alpha_{-p}^\dagger \alpha_q \delta_{-p-q,0} + \alpha_{-k}^\dagger \alpha_q \delta_{-k-q,0} \right) \right) \right) \nonumber \\
%
	&= \dots + \frac{J}{N} \left(  \sum_q\left(\sum_p e^{i(p+q)}\right) \alpha_{q}^\dagger \alpha_q - \frac{1}{2} \left( \sum_k \left( \sum_q \cos(q) \right) \alpha_k^\dagger \alpha_k + \sum_p \left( \sum_q \cos(q) \right) \alpha_p^\dagger \alpha_p + \sum_q \left( \sum_k \cos(k) \right) \alpha_q^\dagger \alpha_q \right) + \sum_q \left( \sum_p \cos(p) \right) \alpha_q^\dagger \alpha_q \right) \nonumber \\
%
	&= \dots + \frac{J}{N} \sum_q \left( \sum_k \left( e^{i(k+q)} - 2 \cos(k) \right) \right) \alpha_q^\dagger \alpha_q
\end{align}
\end{tiny}
Remember that $J < 0$!!



\section{AFM Heisenberg Chain}

\subsection{Real Space}
Hamiltonian:
\begin{align}
	\hat H = J \sum_{\braket{ij}} \vec S_{iA} \cdot \vec S_{jB}
\end{align}
with $J > 0$. \\
The ground state is a Néel state:
\begin{align}
	\vec S_{iA} = S_A \begin{pmatrix}
		0 \\ 0 \\ 1
	\end{pmatrix}
	\qquad
	\vec S_{iB} = S_B \begin{pmatrix}
	0 \\ 0 \\ -1
	\end{pmatrix}
\end{align}
Transform to the local reference frame:
\begin{align}
	\vec S_{iA} = \vec S_{iA}'
	\qquad
	\vec S_{iB} = \begin{pmatrix}
		-1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & -1
	\end{pmatrix} \vec S_{iB}'
\end{align}
And plug into Hamiltonian:
\begin{align}
	\hat H &= J \sum_{\braket{ij}} \left( -(S'_{iA})^x (S'_{jB})^x + (S'_{iA})^y (S'_{jB})^y - (S'_{iA})^z (S'_{jB})^z \right) \\
%
	&= J \sum_{\braket{ij}} \left( -(S'_{iA})^z (S'_{jB})^z - \frac{1}{2} \left( (S'_{iA})^+ (S'_{jB})^+ + (S'_{iA})^- (S'_{jB})^- \right) \right)
\end{align}
HP mapping:
\begin{align}
	(S'_{i\nu})^z &= S_\nu - a_{i\nu}^\dagger a_{i\nu} \\
	%
	(S'_{i\nu})^+ &= \sqrt{2S_\nu - a_{i\nu}^\dagger a_{i\nu}} a_{i\nu} = \sqrt{2} \left(\sqrt{S_\nu} a_{i\nu} - \frac{1}{4\sqrt{S_\nu}} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} + \dots \right) \\
	%
	(S'_{i\nu})^- &= a_{i\nu}^\dagger \sqrt{2S_\nu - a_{i\nu}^\dagger a_{i\nu}} = \sqrt{2} \left(\sqrt{S_\nu} a^\dagger_{i\nu} - \frac{1}{4\sqrt{S_\nu}} a_{i\nu}^\dagger a^\dagger_{i\nu} a_{i\nu} + \dots \right) \\
\end{align}
The Heisenberg term becomes:
\begin{align}
	\dots &= -(S_A - a_{iA}^\dagger a_{iA}) (S_B - a_{jB}^\dagger a_{jB}) \\
	&\ + \left( \left(\sqrt{S_A} a_{iA} - \frac{1}{4 \sqrt{S_A}} a_{iA}^\dagger a_{iA} a_{iA} + \dots \right) \left(\sqrt{S_B} a_{jB} - \frac{1}{4 \sqrt{S_B}} a_{jB}^\dagger a_{jB} a_{jB} + \dots \right) + h.c. \right) \\
%
	&= -S_A S_B + S_{B} a_{iA}^\dagger a_{iA} + S_{A} a_{jB}^\dagger a_{jB} - a_{iA}^\dagger a_{iA} a_{jB}^\dagger a_{jB} \\
	&\ - \left( \sqrt{S_A S_B} a_{iA} a_{jB} - \frac{1}{4} \sqrt{\frac{S_A}{S_B}} a_{iA} a_{jB}^\dagger a_{jB} a_{jB} - \frac{1}{4} \sqrt{\frac{S_B}{S_A}} a_{iA}^\dagger a_{iA} a_{iA} a_{jB} + h.c. + \dots \right)
\end{align}

\subsection{Momentum Space}
Fourier transform:
\begin{align}
	a_i = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_k \hspace{2cm} a_i^\dagger = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_{-k}^\dagger
\end{align}
Quadratic Hamiltonian:
\begin{align}
	\hat H_2 &= J \sum_{\braket{ij}} \left( S_B a_{iA}^\dagger a_{iA} + S_A a_{jB}^\dagger a_{jB} - \sqrt{S_A S_B} a_{iA} a_{jB} - \sqrt{S_A S_B} a_{iB}^\dagger a_{jA}^ \dagger \right) \\
%
	&= J \frac{1}{N} \sum_{kq} \sum_i e^{i(k+q)x_i} \sum_{\delta_{ij} = 0, -1} \left( S_B a_{-kA}^\dagger a_{qA} + e^{i(k+q) \delta_{ij}} S_A a_{-kB}^\dagger a_{qB} -e^{iq \delta_{ij}} \sqrt{S_A S_B} a_{kA} a_{qB} - e^{iq \delta_{ij}} \sqrt{S_A S_B} a_{-kB}^\dagger a_{-qA}^\dagger \right) \\
%
	&= J \sum_{kq} \delta_{k+q,0} \left( 2 S_B a_{-kA}^\dagger a_{qA} + (1 + e^{-i(k+q)}) S_A a_{-kB}^\dagger a_{qB} - (1 + e^{-iq}) \sqrt{S_A S_B} a_{kA} a_{qB} - (1 + e^{-ik}) \sqrt{S_A S_B} a_{-kB}^\dagger a_{-qA}^\dagger \right) \\
%
	&= J \sum_{kq} \delta_{k+q,0} \begin{pmatrix}
		a_{kA} & a_{-kA}^\dagger & a_{kB} & a_{-kB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & 0 & -(1 + e^{-iq}) \sqrt{S_A S_B} & 0 \\
		2S_B & 0 & 0 & 0 \\
		0 & 0 & 0 & 0 \\
		0 & -(1 + e^{-ik}) \sqrt{S_A S_B} & (1+ e^{-i(k+q)}) S_A & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix}
\end{align}
Quartic Hamiltonian:
\begin{align}
	\hat H_4 &= J \sum_{\braket{ij}} \left( -a_{iA}^\dagger a_{iA} a_{jB}^\dagger a_{jB} + \frac{1}{4} \left( \sqrt{\frac{S_A}{S_B}} a_{iA} a_{jB}^\dagger a_{jB} a_{jB} + \sqrt{\frac{S_B}{S_A}} a_{iA}^\dagger a_{iA} a_{iA} a_{jB} + \sqrt{\frac{S_A}{S_B}} a_{jB}^\dagger a_{jB}^\dagger a_{jB} a_{iA}^\dagger + \sqrt{\frac{S_B}{S_A}} a_{jB}^\dagger a_{iA}^\dagger a_{iA}^\dagger a_{iA} \right) \right) \\
%
	&= J \frac{1}{N^2} \sum_{klpq} \sum_{i} e^{i(k+l+p+q)x_i} \sum_{\delta_{ij} = 0,-1} \Bigg( -e^{i(p+q)\delta_{ij}} a_{-kA}^\dagger a_{lA} a_{-pB}^\dagger a_{qB} \\
	&\ + \frac{1}{4} \Bigg( e^{i(l+p+q)\delta_{ij}} \sqrt{\frac{S_A}{S_B}} a_{kA} a_{-lB}^\dagger a_{pB} a_{qB} + e^{iq\delta_{ij}} \sqrt{\frac{S_B}{S_A}} a_{-kA}^\dagger a_{lA} a_{pA} a_{qB} \\
	&\qquad + e^{i(k+l+p)\delta_{ij}} \sqrt{\frac{S_A}{S_B}} a_{-kB}^\dagger a_{-lB}^\dagger a_{pB} a_{-qA}^\dagger + e^{ik\delta_{ij}} \sqrt{\frac{S_B}{S_A}} a_{-kB}^\dagger a_{-lA}^\dagger a_{-pA}^\dagger a_{qA} \Bigg) \Bigg) \\
%
	&= J \frac{1}{N} \sum_{klpq} \delta_{k+l+p+q,0} \Bigg( -(1 + e^{-i(p+q)}) a_{-kA}^\dagger a_{lA} a_{-pB}^\dagger a_{qB} \\
	&\ + \frac{1}{4} \Bigg( (1 + e^{-i(l+p+q)}) \sqrt{\frac{S_A}{S_B}} a_{kA} a_{-lB}^\dagger a_{pB} a_{qB} + (1 + e^{-iq}) \sqrt{\frac{S_B}{S_A}} a_{-kA}^\dagger a_{lA} a_{pA} a_{qB} \\
	&\qquad + (1 + e^{-i(k+l+p)}) \sqrt{\frac{S_A}{S_B}} a_{-kB}^\dagger a_{-lB}^\dagger a_{pB} a_{-qA}^\dagger + (1 + e^{-ik}) \sqrt{\frac{S_B}{S_A}} a_{-kB}^\dagger a_{-lA}^\dagger a_{-pA}^\dagger a_{qA} \Bigg) \Bigg)
\end{align}

\subsection{LSWT}
We have
\begin{footnotesize}
\begin{align}
	\hat H_2 &= J \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & 0 & -(1 + e^{-iq}) \sqrt{S_A S_B} & 0 \\
		2S_B & 0 & 0 & 0 \\
		0 & 0 & 0 & 0 \\
		0 & -(1 + e^{iq}) \sqrt{S_A S_B} & 2S_A & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \nonumber \\ 
%
	 &= J \sum_{q} \begin{pmatrix}
		a_{qA}^\dagger & a_{-qA} & a_{qB}^\dagger & a_{-qB}
	\end{pmatrix} \begin{pmatrix}
	2S_B & 0 & 0 & 0 \\
		0 & 0 & -(1 + e^{-iq}) \sqrt{S_A S_B} & 0 \\
		0 & -(1 + e^{iq}) \sqrt{S_A S_B} & 2S_A & 0 \\
		0 & 0 & 0 & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	 &= J \sum_{q>0} \begin{pmatrix}
		a_{qA}^\dagger & a_{-qA} & a_{qB}^\dagger & a_{-qB}
	\end{pmatrix} \begin{pmatrix}
		2S_B & 0 & 0 & -(1 + e^{-iq}) \sqrt{S_A S_B} \\
		0 & 2S_B & -(1 + e^{-iq}) \sqrt{S_A S_B} & 0 \\
		0 & -(1 + e^{iq}) \sqrt{S_A S_B} & 2S_A & 0 \\
		-(1 + e^{iq}) \sqrt{S_A S_B} & 0 & 0 & 2S_A
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \nonumber 
\end{align}
\end{footnotesize}
The eigenenergies are
\begin{align}
	\epsilon_{q\pm} &= \pm J \left( S_B - S_A \pm \sqrt{(S_A + S_B)^2 - |1 + e^{iq}|^2 S_A S_B} \right) \\
%
	&= \pm J \left( S_B - S_A \pm \sqrt{S_A^2 + S_B^2 - 2 S_A S_B \cos q} \right)
\end{align}
Introduce the eigenmodes:
\begin{align}
	\begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} = \mathcal U(q) \begin{pmatrix}
		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger
	\end{pmatrix}
\end{align}
To find $\mathcal U(q)$, we need to BdG-diagonalize a matrix of the form
\begin{align}
	\begin{pmatrix}
		A & B^* \\
		B & C
	\end{pmatrix}
\end{align}
where $A = 2S_B$, $B = -(1 + e^{iq}) \sqrt{S_A S_B}$, $C = 2S_A$. \\
Eigenvalues are $\epsilon_{\pm} = \pm \frac{A - C}{2} + \sqrt{\left(\frac{A+C}{2}\right)^2 - |B|^2}$. Eigenvectors:
\begin{align}
	\begin{pmatrix}
		A & B^* \\ -B & -C
	\end{pmatrix} \begin{pmatrix}
		\cosh u & \sinh u \\ 
		e^{i\phi} \sinh u & e^{i\phi} \cosh u
	\end{pmatrix} &= \begin{pmatrix}
		A \cosh u + B^* e^{i\phi} \sinh u & A \sinh u + B^* e^{i\phi} \cosh u \\ 
		-B \cosh u - C e^{i\phi} \sinh u & -B \sinh u - C e^{i\phi} \cosh u
	\end{pmatrix} \\
%
	&\overset ! = \begin{pmatrix}
		\epsilon_+ \cosh u & \epsilon_- \sinh u \\ -\epsilon_+ e^{i\phi} \sinh u & -\epsilon_- e^{i\phi} \cosh u
	\end{pmatrix} = \begin{pmatrix}
		\epsilon_+ & 0 \\ 0 & -\epsilon_-
	\end{pmatrix} \begin{pmatrix}
		\cosh u & \sinh u \\ 
		e^{i\phi} \sinh u & e^{i\phi} \cosh u
	\end{pmatrix} \nonumber
\end{align}
\begin{gather}
	\Rightarrow \begin{pmatrix}
		A + |B| \tanh u & A \tanh u + |B| \\
		-|B| - C \tanh u & -|B| \tanh u - C
	\end{pmatrix} = \begin{pmatrix}
		\epsilon_+ & -\epsilon_- \tanh u \\
		\epsilon_+ \tanh u & -\epsilon_-
	\end{pmatrix}
\end{gather}
\begin{gather}
	\Rightarrow \tanh u = \frac{\epsilon_+ - A}{B^* e^{i\phi}} \overset{\phi = \mathrm{arg}(B)}{=} \frac{1}{|B|} \left( -\frac{A + C}{2} + \sqrt{\left(\frac{A+C}{2}\right)^2 - |B|^2} \right) \\
%
	\Rightarrow \sinh u = \frac{\tanh u}{\sqrt{1 - \tanh^2 u}} \qquad \cosh u = \frac{1}{\sqrt{1 - \tanh^2 u}}
\end{gather}

\subsection{Eigenspace}
Quadratic Hamiltonian:
\begin{tiny}
\begin{align}
	\hat H_2 &= J \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & 0 & B^* & 0 \\
		A & 0 & 0 & 0 \\
		0 & 0 & 0 & 0 \\
		0 & B & C & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= J \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & 0 & B^* & 0 \\
		A & 0 & 0 & 0 \\
		0 & 0 & 0 & 0 \\
		0 & B & C & 0
	\end{pmatrix} \begin{pmatrix}
		\cosh u & 0 & 0 & \sinh u \\
		0 & \cosh u & \sinh u & 0 \\
		0 & e^{i\phi_q} \sinh u & e^{i\phi_q} \cosh u & 0 \\
		e^{i\phi_q} \sinh u & 0 & 0 & e^{i\phi_q} \cosh u
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= J \sum_{q} \begin{pmatrix}
		\alpha_{-qA} & \alpha_{qA}^\dagger & \alpha_{-qB} & \alpha_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		\cosh u & 0 & 0 & e^{i\phi_{-q}} \sinh u \\
		0 & \cosh u &  e^{i\phi_{-q}} \sinh u & 0 \\
		0 &\sinh u & e^{i\phi_{-q}} \cosh u & 0 \\
		\sinh u & 0 & 0 & e^{i\phi_{-q}} \cosh u
	\end{pmatrix} \begin{pmatrix}
		0 & B^* e^{i\phi_q} \sinh u & B^* e^{i\phi_q} \cosh u & 0 \\
		A \cosh u & 0 & 0 & A \sinh u \\
		0 & 0 & 0 & 0 \\
		0 & B \cosh u + C e^{i\phi_q} \sinh u & B \sinh u + C e^{i\phi_q} \cosh u & 0
	\end{pmatrix}  \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= J \sum_{q} \begin{pmatrix}
		\alpha_{-qA} & \alpha_{qA}^\dagger & \alpha_{-qB} & \alpha_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & 2 |B| \sinh u \cosh u + C \sinh^2 u & |B| (\cosh^2 u + \sinh^2 u) + C \sinh u \cosh u & 0 \\
		A \cosh^2 u & 0 & 0 & A \sinh u \cosh u \\
		A \sinh u \cosh u & 0 & 0 & A \sinh^2 u \\
		0 & |B| (\cosh^2 u + \sinh^2 u) + C \sinh u \cosh u & 2 |B| \sinh u \cosh u + C \cosh^2 u & 0
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= J \sum_{q>0} \begin{pmatrix}
		\alpha_{-qA} & \alpha_{qA}^\dagger & \alpha_{-qB} & \alpha_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & v & x & 0 \\
		v & 0 & 0 & x \\
		x & 0 & 0 & w \\
		0 & x & w & 0
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= J \sum_{q>0} \begin{pmatrix}
		\alpha_{-qA} & \alpha_{qA}^\dagger & \alpha_{-qB} & \alpha_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & \epsilon_+ & 0 & 0 \\
		\epsilon_+ & 0 & 0 & 0 \\
		0 & 0 & 0 & \epsilon_- \\
		0 & 0 & \epsilon_- & 0
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber
\end{align}
\end{tiny}
where we used $\phi_q \equiv \operatorname{arg}(B)$ and $\phi_{-q} \equiv \operatorname{arg}(B^*) = -\phi_q$.
We also used the eigenvalue equation to define
\begin{align}
	v &\equiv A \cosh^2 u + C \sinh^2 u + 2|B| \sinh u \cosh u \\
	&= \frac{A + C \tanh^2 u + 2|B| \tanh u}{1 - \tanh^2 u} \\
	&= \frac{(A + |B| \tanh u) + \tanh u (C \tanh u + |B|)}{1 - \tanh^2 u} \\
	&= \epsilon_+
\end{align}
\begin{align}	
	w &\equiv A \sinh^2 u + C \cosh^2 u + 2|B| \sinh u \cosh u \\
	&= \frac{A \tanh^2 u + C + 2|B| \tanh u}{1 - \tanh^2 u} \\
	&= \frac{\tanh u (A \tanh u + |B|) + (|B| \tanh u + C)}{1 - \tanh^2 u} \\
	&= \epsilon_-
\end{align}
\begin{align}
	x &\equiv (A + C) \sinh u \cosh u + |B| (\cosh^2 u + \sinh^2 u) \\
	&= \frac{(A + C) \tanh u + |B| (1 + \tanh^2 u)}{1 - \tanh^2 u} \\
	&= \frac{\tanh u (A + |B| \tanh u) + |B| + C \tanh u}{1 - \tanh^2 u} \\
	&= \frac{\tanh u \, \epsilon_+ - \tanh u \, \epsilon_+}{1 - \tanh^2 u} \\
	&= 0
\end{align}
Quartic Hamiltonian:
\begin{align}
	\hat H_4 &= \frac{J}{N} \sum_{kpq} \Bigg( -(1 + e^{-i(p+q)}) a_{k+p+qA}^\dagger a_{kA} a_{-pB}^\dagger a_{qB} \\
	&\ + \frac{1}{4} \Bigg( (1 + e^{-i(k+p+q)}) \sqrt{\frac{S_A}{S_B}} a_{-k-p-qA} a_{-kB}^\dagger a_{pB} a_{qB} + (1 + e^{-iq}) \sqrt{\frac{S_B}{S_A}} a_{k+p+qA}^\dagger a_{kA} a_{pA} a_{qB} \\
	&\qquad + (1 + e^{iq}) \sqrt{\frac{S_A}{S_B}} a_{k+p+qB}^\dagger a_{-kB}^\dagger a_{pB} a_{-qA}^\dagger + (1 + e^{i(k+p+q)}) \sqrt{\frac{S_B}{S_A}} a_{k+p+qB}^\dagger a_{-kA}^\dagger a_{-pA}^\dagger a_{qA} \Bigg) \Bigg) \\
%
	\dots
\end{align}


\section{Honeycomb Heisenberg DMI}

\subsection{Real Space}

Hamiltonian:
\begin{align}
	\hat H &= J \sum_{\braket{ij}} \vec S_{iA} \cdot \vec S_{jB} + D \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu (S^x_{i\nu} S^y_{j\nu} - S^y_{i\nu} S^x_{j\nu})
\end{align}
with $J < 0$. \\
The ground state is a fully polarized ferromagnet:
\begin{align}
	\vec S_{iA} = S_A \begin{pmatrix}
		\sin\theta \\ 0 \\ \cos\theta
	\end{pmatrix}
	\qquad
	\vec S_{iB} = S_B \begin{pmatrix}
		\sin\theta \\ 0 \\ \cos\theta
	\end{pmatrix}
\end{align}
Transform to the local reference frame:
\begin{align}
	\vec S_{i\nu} = \begin{pmatrix}
		\cos\theta & 0 & \sin\theta \\
		0 & 1 & 0 \\
		-\sin\theta & 0 & \cos\theta
	\end{pmatrix} \vec S'_{i\nu}
\end{align}
And plug into Hamiltonian:
\begin{align}
	\hat H &= J \sum_{\braket{ij}} \vec S'_{iA} \cdot \vec S'_{jB} + D \cos\theta \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu ((S'_{i\nu})^x (S'_{j\nu})^y - (S'_{i\nu})^y (S'_{j\nu})^x) \\
	&\hspace{2.7cm} + D \sin\theta \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu ((S'_{i\nu})^z (S'_{j\nu})^y - (S'_{i\nu})^y (S'_{j\nu})^z)
\end{align}
HP mapping:
\begin{align}
	(S'_{i\nu})^z &= S_\nu - a_{i\nu}^\dagger a_{i\nu} \\
%
	(S'_{i\nu})^+ &= \sqrt{2S_\nu - a_{i\nu}^\dagger a_{i\nu}} a_{i\nu} = \sqrt{2} \left(\sqrt{S_\nu} a_{i\nu} - \frac{1}{4\sqrt{S_\nu}} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} + \dots \right) \\
%
	(S'_{i\nu})^- &= a_{i\nu}^\dagger \sqrt{2S_\nu - a_{i\nu}^\dagger a_{i\nu}} = \sqrt{2} \left(\sqrt{S_\nu} a^\dagger_{i\nu} - \frac{1}{4\sqrt{S_\nu}} a_{i\nu}^\dagger a^\dagger_{i\nu} a_{i\nu} + \dots \right) \\
\end{align}
The Heisenberg term becomes:
\begin{align}
	\dots &= (S'_{iA})^z (S'_{jB})^z + \frac{1}{2} ((S'_{iA})^+ (S'_{jB})^- + h.c.) \\
%
	&= (S_A - a_{iA}^\dagger a_{iA}) (S_B - a_{iB}^\dagger a_{iB}) \\
	&\ + \left( \left(\sqrt{S_A} a_{iA} - \frac{1}{4\sqrt{S_A}} a_{iA}^\dagger a_{iA} a_{iA} + \dots \right) \left(\sqrt{S_B} a^\dagger_{jB} - \frac{1}{4\sqrt{S_B}} a_{jB}^\dagger a^\dagger_{jB} a_{jB} + \dots \right) + h.c. \right) \\
%
	&= S_A S_B - S_B a_{iA}^\dagger a_{iA} - S_A a_{jB}^\dagger a_{jB} + a_{iA}^\dagger a_{iA} a_{jB}^\dagger a_{jB} \\
	&\ + \left(\sqrt{S_A S_B} a_{iA} a_{jB}^\dagger - \frac{1}{4}\sqrt{\frac{S_A}{S_B}} a_{iA} a_{jB}^\dagger a_{jB}^\dagger a_{jB} - \frac{1}{4}\sqrt{\frac{S_B}{S_A}} a_{iA}^\dagger a_{iA} a_{iA} a_{jB}^\dagger + h.c. + \dots \right)
\end{align}
The $xy$-DMI term becomes:
\begin{align}
	\dots &= \frac{i}{2} (S'_{i\nu})^+ (S'_{j\nu})^- - h.c. \\
%
	&= i \left(S_\nu a_{i\nu} a_{j\nu}^\dagger - \frac{1}{4} a_{i\nu} a_{j\nu}^\dagger a_{j\nu}^\dagger a_{j\nu} - \frac{1}{4} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} a_{j\nu}^\dagger - h.c. + \dots \right)
\end{align}
The $zy$-DMI term becomes:
\begin{align}
	\dots &= \frac{1}{i\sqrt 2} \bigg((S_\nu - a_{i\nu}^\dagger a_{i\nu}) \left(\sqrt{S_\nu} a_{j\nu} - \frac{1}{4\sqrt{S_\nu}} a_{j\nu}^\dagger a_{j\nu} a_{j\nu} + \dots \right) \\
	&\hspace{1cm} - \left(\sqrt{S_\nu} a_{i\nu} - \frac{1}{4\sqrt{S_\nu}} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} + \dots \right) (S_\nu - a_{j\nu}^\dagger a_{j\nu}) \bigg) - h.c. \\
%
	&= \frac{1}{i\sqrt 2} \left(S_\nu^{3/2} (a_{j\nu} - a_{i\nu}) - \sqrt{S_\nu} \left(a_{i\nu}^\dagger a_{i\nu} a_{j\nu} + \frac{1}{4} a_{j\nu}^\dagger a_{j\nu} a_{j\nu} - a_{i\nu} a_{j\nu}^\dagger a_{j\nu} - \frac{1}{4} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} \right) \right) - h.c. \\
%
	&= \frac{i}{\sqrt 2} \left(S_\nu^{3/2} (a_{i\nu} - a_{j\nu}) + \sqrt{S_\nu} \left(a_{i\nu}^\dagger a_{i\nu} a_{j\nu} + \frac{1}{4} a_{j\nu}^\dagger a_{j\nu} a_{j\nu} - a_{i\nu} a_{j\nu}^\dagger a_{j\nu} - \frac{1}{4} a_{i\nu}^\dagger a_{i\nu} a_{i\nu} \right) \right) - h.c.
\end{align}
with n.n.\ vectors $\vec \delta^1_n$ and n.n.n.\ vectors $\vec \delta^2_n$, which are related to the Bravais lattice vectors $\vec a_{1,2}$ as follows:
\begin{gather}
	\vec\delta^1_0 = 0 \qquad \vec\delta^1_1 = \vec a_1 \qquad \vec\delta^1_2 = \vec a_2 \\
%
	\vec\delta^2_1 = \vec a_1 - \vec a_2 \qquad \vec\delta^2_2 = -\vec a_1 \qquad \vec\delta^2_3 = \vec a_2
\end{gather}

\subsection{Momentum Space}
Fourier transform:
\begin{align}
	a_i = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_k \hspace{2cm} a_i^\dagger = \frac{1}{\sqrt N} \sum_k e^{ikx_i} a_{-k}^\dagger
\end{align}
Linear Hamiltonian:
\begin{align}
	\hat H_1 &= D \sin\theta \frac{i}{\sqrt 2} \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu S_\nu^{3/2} (a_{i\nu} - a_{j\nu} - a_{i\nu}^\dagger + a_{j\nu}^\dagger) \\
%
	&= D \sin\theta \frac{i}{\sqrt 2} \frac{1}{\sqrt N} \sum_k \sum_{i} e^{ikx_i} \sum_{\nu, \delta_{ij}^2} (-1)^\nu S_\nu^{3/2} (a_{k\nu} - e^{ik\delta_{ij}^2} a_{k\nu} - a_{-k\nu}^\dagger + e^{ik\delta_{ij}^2} a_{-k\nu}^\dagger) \\
%
	&= D \sin\theta \frac{i}{\sqrt 2} \sqrt N \sum_k \delta_{k0} \sum_{\nu, \delta_{ij}^2} (-1)^\nu S_\nu^{3/2} ((1 - e^{ik\delta_{ij}^2}) a_{k\nu} - (1 - e^{ik\delta_{ij}^2}) a_{-k\nu}^\dagger) \\
%
	&= D \sin\theta \frac{i}{\sqrt 2} \sqrt N \sum_k \delta_{k0} \sum_{\nu} (-1)^\nu S_\nu^{3/2} \underbrace{\left(3 - \sum_{\delta_{ij}^2} e^{ik\delta_{ij}^2}\right)}_{\equiv \gamma_{k}} (a_{k\nu} - a_{-k\nu}^\dagger) \\
%
	&= D \sin\theta \frac{i}{\sqrt 2} \sqrt N \sum_k \delta_{k0} \gamma_k \left(S_A^{3/2} a_{kA} - S_A^{3/2} a_{-kA}^\dagger - S_B^{3/2} a_{kB} + S_B^{3/2} a_{-kB}^\dagger \right)
\end{align}
Quadratic Hamiltonian:
\begin{align}
	\hat H_2 &= J \sum_{\braket{ij}} \left( - S_B a_{iA}^\dagger a_{iA} - S_A a_{iB}^\dagger a_{iB} + \sqrt{S_A S_B} a_{iA} a_{jB}^\dagger + \sqrt{S_A S_B} a_{jB} a_{iA}^\dagger \right) + i D \cos\theta \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu S_\nu \left( a_{i\nu} a_{j\nu}^\dagger - a_{j\nu} a_{i\nu}^\dagger \right) \\
%
	&= \frac{1}{N} \sum_{kq} \sum_i e^{i(k+q)x_i} \Bigg[ J \sum_{\delta_{ij}^1} \left( -S_B a_{-kA}^\dagger a_{qA} - e^{i(k+q)\delta_{ij}^1} S_A a_{-kB}^\dagger a_{qB} + e^{iq\delta_{ij}^1} \sqrt{S_A S_B} a_{kA} a_{-qB}^\dagger + e^{ik\delta_{ij}^1} \sqrt{S_A S_B} a_{kB} a_{-qA}^\dagger \right) \\
	&\hspace{2.2cm} + iD\cos\theta \sum_{\nu,\delta_{ij}^2} (-1)^\nu S_\nu \left( e^{iq\delta_{ij}^2} a_{k\nu} a_{-q\nu}^\dagger - e^{ik\delta_{ij}^2} a_{k\nu} a_{-q\nu}^\dagger \right) \Bigg] \\
%
	&= \sum_{kq} \delta_{k+q,0} \Bigg[ J \left( -3 S_B a_{-kA}^\dagger a_{qA} - \beta_{k+q}^1 S_A a_{-kB}^\dagger a_{qB} + \beta_q^1 \sqrt{S_A S_B} a_{kA} a_{-qB}^\dagger + \beta_k^1 \sqrt{S_A S_B} a_{kB} a_{-qA}^\dagger \right) \\
	&\hspace{1.7cm} + iD\cos\theta \sum_\nu (-1)^\nu (\beta_q^2 - \beta_k^2) S_\nu a_{k\nu} a_{-q\nu}^\dagger \Bigg] \\
%
	&= \sum_{kq} \delta_{k+q,0} \begin{pmatrix}
		a_{kA} & a_{-kA}^\dagger & a_{kB} & a_{-kB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & iD\cos\theta (\beta_q^2 - \beta_k^2) S_A & 0 & J \beta_q^1 \sqrt{S_A S_B} \\
		-3J S_B & 0 & 0 & 0 \\
		0 & J \beta_k^1 \sqrt{S_A S_B} & 0 & -iD\cos\theta (\beta_q^2 - \beta_k^2) S_B \\
		0 & 0 & -J \beta_{k+q}^2 S_A & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix}
\end{align}
where $\beta_k^{1,2} \equiv \sum_{\delta_{ij}^{1,2}} e^{ik\delta_{ij}^{1,2}}$. \\
Cubic Hamiltonian:
\begin{align}
	\hat H_3 &= \frac{i}{\sqrt 2} D \sin\theta \sum_{\braket{\braket{ij}}_\nu} (-1)^\nu \sqrt{S_\nu} \bigg( a_{i\nu}^\dagger a_{i\nu} a_{j\nu} - a_{i\nu} a_{j\nu}^\dagger a_{j\nu} - a_{j\nu}^\dagger a_{i\nu}^\dagger a_{i\nu} + a_{j\nu}^\dagger a_{j\nu} a_{i\nu}^\dagger \\
	&\hspace{3.9cm} + \frac{1}{4} \left( a_{j\nu}^\dagger a_{j\nu} a_{j\nu} - a_{i\nu}^\dagger a_{i\nu} a_{i\nu} - a_{j\nu}^\dagger a_{j\nu}^\dagger a_{j\nu} + a_{i\nu}^\dagger a_{i\nu}^\dagger a_{i\nu}  \right) \bigg) \\
%
	&= \frac{i D \sin\theta}{\sqrt{2N^3}}  \sum_{kpq} \sum_i e^{i(k+p+q)x_i} \sum_{\nu} (-1)^\nu \sqrt{S_\nu} \bigg( \beta_q^2 a_{-k\nu}^\dagger a_{p\nu} a_{q\nu} - \beta_{p+q}^2 a_{k\nu} a_{-p\nu}^\dagger a_{q\nu} - \beta_{k}^2 a_{-k\nu}^\dagger a_{-p\nu}^\dagger a_{q\nu} + \beta_{k+p}^2 a_{-k\nu}^\dagger a_{p\nu} a_{-q\nu}^\dagger \\
	&\hspace{1cm} +\frac{1}{4} \left( \beta_{k+p+q}^2 a_{-k\nu}^\dagger a_{p\nu} a_{q\nu} - 3 a_{-k\nu}^\dagger a_{p\nu} a_{q\nu} - \beta_{k+p+q}^2 a_{-k\nu}^\dagger a_{-p\nu}^\dagger a_{q\nu} + 3 a_{-k\nu}^\dagger a_{p\nu} a_{-q\nu}^\dagger \right) \bigg) \\
%
	&= \frac{i D \sin\theta}{\sqrt{2N}} \sum_{kpq} \delta_{k+p+q,0} \sum_\nu (-1)^\nu \sqrt{S_\nu} \bigg( \left( \beta_q^2 + \frac{1}{4} \left( \beta_{k+p+q}^2 - 3 \right) \right) a_{-k\nu}^\dagger a_{p\nu} a_{q\nu} - \left( \beta_k^2 + \frac{1}{4} \left( \beta_{k+p+q}^2 - 3 \right) \right) a_{-k\nu}^\dagger a_{-p\nu}^\dagger a_{q\nu} \\
	&\hspace{1cm} - \beta_{p+q}^2 a_{k\nu} a_{-p\nu}^\dagger a_{q\nu} + \beta_{k+p}^2 a_{-k\nu}^\dagger a_{p\nu} a_{-q\nu}^\dagger \bigg)
\end{align}
\dots

\subsection{LSWT}
We have
\begin{tiny}
\begin{align}
	\hat H_2 &= \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_A & 0 & J \beta_q^1 \sqrt{S_A S_B} \\
		-3J S_B & 0 & 0 & 0 \\
		0 & J \beta_{-q}^1 \sqrt{S_A S_B} & 0 & -iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_B \\
		0 & 0 & -3J S_A & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \\
%
	&= \sum_{q} \begin{pmatrix}
		a_{qA}^\dagger & a_{-qA} & a_{qB}^\dagger & a_{-qB}
	\end{pmatrix} \begin{pmatrix}
		-3J S_B & 0 & 0 & 0 \\
		0 & iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_A & 0 & J \beta_q^1 \sqrt{S_A S_B} \\
		0 & 0 & -3J S_A & 0 \\
		0 & J \beta_{-q}^1 \sqrt{S_A S_B} & 0 & -iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_B
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \\
%
	&= \dots \begin{pmatrix}
		-3J S_B + \frac{1}{2} iD\cos\theta (\beta_{-q}^2 - \beta_{q}^2) S_A & 0 & J \beta_q^1 \sqrt{S_A S_B} & 0 \\
		0 & -3JS_B + iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_A & 0 & J \beta_q^1 \sqrt{S_A S_B} \\
		J \beta_{-q}^1 \sqrt{S_A S_B} & 0 & -3J S_A - iD\cos\theta (\beta_{-q}^2 - \beta_{q}^2) S_B & 0 \\
		0 & J \beta_{-q}^1 \sqrt{S_A S_B} & 0 & -3J S_A - iD\cos\theta (\beta_q^2 - \beta_{-q}^2) S_B
	\end{pmatrix} \dots \nonumber \\
%
	&\equiv \begin{pmatrix}
		-3JS_B + b(-q) S_A & 0 & a(q) & 0 \\ 0 & -3JS_B + b(q) S_A & 0 & a(q) \\ a(-q) & 0 & -3JS_A - b(-q) S_B & 0 \\ 0 & a(-q) & 0 & -3JS_A - b(q) S_B
	\end{pmatrix}
\end{align}
\end{tiny}
where $a(q) \equiv J\beta_q^1 \sqrt{S_A S_B}$ and $b(q) \equiv iD\cos\theta (\beta_q^2 - \beta_{-q}^2)$. \\
The eigenvalues are
\begin{align}
	\epsilon_q = \begin{cases}
		\frac{1}{2} \left( -3J (S_B + S_A) + B(-q) (S_A - S_B) \pm \sqrt{(-3J (S_B - S_A) + B(-q) (S_A + S_B))^2 + 4|A(q)|^2} \right) \\
		\frac{1}{2} \left( -3J (S_B + S_A) + B(q) (S_A - S_B) \pm \sqrt{(-3J (S_B - S_A) + B(q) (S_A + S_B))^2 + 4|A(q)|^2} \right)
	\end{cases}
\end{align}Introduce the eigenmodes:
\begin{align}
	\begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} = \mathcal U(q) \begin{pmatrix}
		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger
	\end{pmatrix}
\end{align}
To find $\mathcal U(q)$, we need to unitarily diagonalize a matrix of the form
\begin{align}
	\begin{pmatrix}
		A & B^* \\
		B & C
	\end{pmatrix}
\end{align}
where $A_{1,2} = -3JS_B + b(\mp q) S_A$, $B = a(q)$, $C_{1,2} = -3JS_A - b(\mp q)S_B$. We drop the $1,2$ label for now. \\
Eigenvalues are $\epsilon_{\pm} = \frac{A + C}{2} \pm \sqrt{\left(\frac{A-C}{2}\right)^2 + |B|^2}$. Eigenvectors:
\begin{gather}
	\begin{pmatrix}
		A & B^* \\ B & C
	\end{pmatrix} \begin{pmatrix}
		-\sin u & \cos u \\ 
		e^{i\phi} \cos u & e^{i\phi} \sin u
	\end{pmatrix} = \begin{pmatrix}
		-A \sin u + B^* e^{i\phi} \cos u & A \cos u + B^* e^{i\phi} \sin u \\ 
		-B \sin u + C e^{i\phi} \cos u & B \cos u + C e^{i\phi} \sin u
	\end{pmatrix} \overset ! = \begin{pmatrix}
		-\epsilon_- \sin u & \epsilon_+ \cos u \\ 
		\epsilon_- e^{i\phi} \cos u & \epsilon_+ e^{i\phi} \sin u
	\end{pmatrix} \nonumber \\
	%
	\Rightarrow \tan u = \frac{\epsilon_+ - A}{B^* e^{i\phi}} \overset{\phi = \mathrm{arg}(B)}{=} \frac{1}{|B|} \left( -\frac{A - C}{2} + \sqrt{\left(\frac{A-C}{2}\right)^2 + |B|^2} \right) \\
	%
	\Rightarrow \sin u = \frac{\tan u}{\sqrt{1 + \tan^2 u}} \qquad \cos u = \frac{1}{\sqrt{1 + \tan^2 u}}
\end{gather}
We note that for $q \to -q$, we have $B \to B^*$ and so $\phi \to -\phi$.

\subsection{Eigenspace}
Linear Hamiltonian:
\begin{align}
	\hat H_1 &\propto \gamma_0 = 0
\end{align}
as it should be. Quadratic Hamiltonian:
\begin{tiny}
\begin{align}
	\hat H_2 &= \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & b(q) S_A & 0 & a(q) \\
		-3J S_B & 0 & 0 & 0 \\
		0 & a(-q) & 0 & b(-q) S_B \\
		0 & 0 & -3J S_A & 0
	\end{pmatrix} \begin{pmatrix}
		a_{qA} \\ a_{-qA}^\dagger \\ a_{qB} \\ a_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= \sum_{q} \begin{pmatrix}
		a_{-qA} & a_{qA}^\dagger & a_{-qB} & a_{qB}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & b(q) S_A & 0 & a(q) \\
		-3J S_B & 0 & 0 & 0 \\
		0 & a(-q) & 0 & -b(q) S_B \\
		0 & 0 & -3J S_A & 0
	\end{pmatrix} \begin{pmatrix}
		-\sin u_1(q) & 0 & \cos u_1(q) & 0 \\
		0 & -\sin u_2(q) & 0 & \cos u_2(q) \\
		e^{i\phi} \cos u_1(q) & 0 & e^{i\phi} \sin u_1(q) & 0 \\
		0 & e^{i\phi} \cos u_2(q) & 0 & e^{i\phi} \sin u_2(q)
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qA} \\ \alpha_{-qA}^\dagger \\ \alpha_{qB} \\ \alpha_{-qB}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= \sum_{q} \begin{pmatrix}
		\alpha_{-q1} & \alpha_{q1}^\dagger & \alpha_{-q2} & \alpha_{q2}^\dagger
	\end{pmatrix} \begin{pmatrix}
		-\sin u_1(-q) & 0 & e^{-i\phi} \cos u_1(-q) & 0 \\
		0 & -\sin u_2(-q) & 0 & e^{-i\phi} \cos u_2(-q) \\
		\cos u_1(-q) & 0 & e^{-i\phi} \sin u_1(-q) & 0 \\
		0 & \cos u_2(-q) & 0 & e^{-i\phi} \sin u_2(-q)
	\end{pmatrix} \\
	&\hspace{3cm} \begin{pmatrix}
		0 & -b(q) S_A \sin u_2(q) + a(q) e^{i\phi} \cos u_2(q) & 0 & b(q) S_A \cos u_2(q) + a(q) e^{i\phi} \sin u_2(q) \\
		3JS_B \sin u_1(q) & 0 & -3JS_B \cos u_1(q) & 0 \\
		0 & -a(-q) \sin u_2(q) - b(q) S_B e^{i\phi} \cos u_2(q) & 0 & a(-q) \cos u_2(q) - b(q) S_B e^{i\phi} \sin u_2(q) \\
		-3JS_A e^{i\phi} \cos u_1(q) & 0 & -3JS_A e^{i\phi} \sin u_1(q) & 0
	\end{pmatrix} \begin{pmatrix}
		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= \sum_{q} \begin{pmatrix}
		\alpha_{-q1} & \alpha_{q1}^\dagger & \alpha_{-q2} & \alpha_{q2}^\dagger
	\end{pmatrix} \begin{pmatrix}
		0 & x & 0 & y \\
		\dots
	\end{pmatrix} \begin{pmatrix}
		\alpha_{q1} \\ \alpha_{-q1}^\dagger \\ \alpha_{q2} \\ \alpha_{-q2}^\dagger
	\end{pmatrix} \nonumber \\
\end{align}
\end{tiny}
where we have defined
\begin{align}
	x &= b(q) S_A \sin u_1(-q) \sin u_2(q) - a(q) e^{i\phi} \sin u_1(-q) \cos u_2(q) - a(-q) e^{-i\phi} \sin u_2(q) \cos u_1(-q) - b(q) S_B \cos u_1(-q) \cos u_2(q) \nonumber \\
%
	y &= -b(q) S_A \sin u_1(-q) \cos u_2(q) - a(q) e^{i\phi} \sin u_1(-q) \sin u_2(q) + a(-q) e^{-i\phi} \cos u_1(-q) \cos u_2(q) - b(q) S_B \cos u_1(-q) \sin u_2(q) \nonumber \\
%
	\dots
\end{align}

\subsection{Normal Order}
Quadratic Hamiltonian in eigenspace:
\begin{align}
	\hat H_2 &= \sum_q \sum_{n=1}^2 \begin{pmatrix}
		\alpha_{-qn} & \alpha_{qn}^\dagger
	\end{pmatrix} \begin{pmatrix}
		g^{--}_{-q,q;n} & g^{-+}_{-q,q;n} \\
		g^{+-}_{-q,q;n} & g^{++}_{-q,q;n} \\
	\end{pmatrix} \begin{pmatrix}
		\alpha_{qn} \\ \alpha_{-qn}^\dagger
	\end{pmatrix}
	= \sum_q \sum_{n=1}^2 \begin{pmatrix}
		\alpha_{qn} & \alpha_{-qn}^\dagger
	\end{pmatrix} \begin{pmatrix}
		g^{--}_{q,-q;n} & g^{-+}_{q,-q;n} \\
		g^{+-}_{q,-q;n} & g^{++}_{q,-q;n} \\
	\end{pmatrix} \begin{pmatrix}
		\alpha_{-qn} \\ \alpha_{qn}^\dagger
	\end{pmatrix} \nonumber \\
%
	&= \sum_q \sum_{n=1}^2 \bigg( \frac{1}{2} (g^{--}_{-q,q;n} + g^{--}_{q,-q;n}) \alpha_{-qn} \alpha_{qn} + \underbrace{(g^{-+}_{q,-q;n} + g^{+-}_{-q,q;n}) \alpha_{qn}^\dagger \alpha_{-qn}}_{\text{or} (g^{-+}_{-q,q;n} + g^{+-}_{q,-q;n}) \alpha_{qn} \alpha_{-qn}^\dagger} + \frac{1}{2} (g^{++}_{-q,q;n} + g^{++}_{q,-q;n}) \alpha_{qn}^\dagger \alpha_{-qn}^\dagger \bigg) + \sum_q \sum_{n=1}^2 g^{-+}_{-q,q;n}
\end{align}

\end{document}